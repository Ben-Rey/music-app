{
  "name": "music-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "concurrently": "^6.0.0"
  },
  "scripts": {
    "start": "concurrently  \"npm run start --prefix front\" \"npm run dev --prefix back\""
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {}
}
